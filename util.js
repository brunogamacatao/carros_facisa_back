function subtrai(a, b) {
  return a - b;
}

const soma = function (a, b) {
  return a + b;
};

const multiplica = (a, b) => {
  return a * b;
};

const divide = (a, b) => a / b;

module.exports = {
  soma, 
  multiplica
};